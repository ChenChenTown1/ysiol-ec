#!/bin/bash
for file in *; do
    [ -f "$file" ] || continue
    new_name="$file"
    new_name="${new_name//.ab/}"
    new_name="${new_name//.aa/}"
    new_name="${new_name//.af/}"
    new_name="${new_name//.ak/}"
    new_name="${new_name//.sq/}"
    new_name="${new_name//.am/}"
    new_name="${new_name//.ar/}"
    new_name="${new_name//.hy/}"
    new_name="${new_name//.as/}"
    new_name="${new_name//.ay/}"
    new_name="${new_name//.az/}"
    new_name="${new_name//.bn/}"
    new_name="${new_name//.ba/}"
    new_name="${new_name//.eu/}"
    new_name="${new_name//.be/}"
    new_name="${new_name//.bho/}"
    new_name="${new_name//.bs/}"
    new_name="${new_name//.br/}"
    new_name="${new_name//.bg/}"
    new_name="${new_name//.my/}"
    new_name="${new_name//.ca/}"
    new_name="${new_name//.ceb/}"
    new_name="${new_name//.zh-Hans/}"
    new_name="${new_name//.zh-Hant/}"
    new_name="${new_name//.co/}"
    new_name="${new_name//.hr/}"
    new_name="${new_name//.cs/}"
    new_name="${new_name//.da/}"
    new_name="${new_name//.dv/}"
    new_name="${new_name//.nl/}"
    new_name="${new_name//.dz/}"
    new_name="${new_name//.en-orig/}"
    new_name="${new_name//.en/}"
    new_name="${new_name//.eo/}"
    new_name="${new_name//.et/}"
    new_name="${new_name//.ee/}"
    new_name="${new_name//.fo/}"
    new_name="${new_name//.fj/}"
    new_name="${new_name//.fil/}"
    new_name="${new_name//.fi/}"
    new_name="${new_name//.fr/}"
    new_name="${new_name//.gaa/}"
    new_name="${new_name//.gl/}"
    new_name="${new_name//.lg/}"
    new_name="${new_name//.ka/}"
    new_name="${new_name//.de/}"
    new_name="${new_name//.el/}"
    new_name="${new_name//.gn/}"
    new_name="${new_name//.gu/}"
    new_name="${new_name//.ht/}"
    new_name="${new_name//.ha/}"
    new_name="${new_name//.haw/}"
    new_name="${new_name//.iw/}"
    new_name="${new_name//.hi/}"
    new_name="${new_name//.hmn/}"
    new_name="${new_name//.hu/}"
    new_name="${new_name//.is/}"
    new_name="${new_name//.ig/}"
    new_name="${new_name//.id/}"
    new_name="${new_name//.iu/}"
    new_name="${new_name//.ga/}"
    new_name="${new_name//.it/}"
    new_name="${new_name//.ja/}"
    new_name="${new_name//.jv/}"
    new_name="${new_name//.kl/}"
    new_name="${new_name//.kn/}"
    new_name="${new_name//.kk/}"
    new_name="${new_name//.kha/}"
    new_name="${new_name//.km/}"
    new_name="${new_name//.rw/}"
    new_name="${new_name//.ko/}"
    new_name="${new_name//.kri/}"
    new_name="${new_name//.ku/}"
    new_name="${new_name//.ky/}"
    new_name="${new_name//.lo/}"
    new_name="${new_name//.la/}"
    new_name="${new_name//.lv/}"
    new_name="${new_name//.ln/}"
    new_name="${new_name//.lt/}"
    new_name="${new_name//.lua/}"
    new_name="${new_name//.luo/}"
    new_name="${new_name//.lb/}"
    new_name="${new_name//.mk/}"
    new_name="${new_name//.mg/}"
    new_name="${new_name//.ms/}"
    new_name="${new_name//.ml/}"
    new_name="${new_name//.mt/}"
    new_name="${new_name//.gv/}"
    new_name="${new_name//.mi/}"
    new_name="${new_name//.mr/}"
    new_name="${new_name//.mn/}"
    new_name="${new_name//.mfe/}"
    new_name="${new_name//.ne/}"
    new_name="${new_name//.new/}"
    new_name="${new_name//.nso/}"
    new_name="${new_name//.no/}"
    new_name="${new_name//.ny/}"
    new_name="${new_name//.oc/}"
    new_name="${new_name//.or/}"
    new_name="${new_name//.om/}"
    new_name="${new_name//.os/}"
    new_name="${new_name//.pam/}"
    new_name="${new_name//.ps/}"
    new_name="${new_name//.fa/}"
    new_name="${new_name//.pl/}"
    new_name="${new_name//.pt/}"
    new_name="${new_name//.pt-PT/}"
    new_name="${new_name//.pa/}"
    new_name="${new_name//.qu/}"
    new_name="${new_name//.ro/}"
    new_name="${new_name//.rn/}"
    new_name="${new_name//.ru/}"
    new_name="${new_name//.sm/}"
    new_name="${new_name//.sg/}"
    new_name="${new_name//.sa/}"
    new_name="${new_name//.gd/}"
    new_name="${new_name//.sr/}"
    new_name="${new_name//.crs/}"
    new_name="${new_name//.sn/}"
    new_name="${new_name//.sd/}"
    new_name="${new_name//.si/}"
    new_name="${new_name//.sk/}"
    new_name="${new_name//.sl/}"
    new_name="${new_name//.so/}"
    new_name="${new_name//.st/}"
    new_name="${new_name//.es/}"
    new_name="${new_name//.su/}"
    new_name="${new_name//.sw/}"
    new_name="${new_name//.ss/}"
    new_name="${new_name//.sv/}"
    new_name="${new_name//.tg/}"
    new_name="${new_name//.ta/}"
    new_name="${new_name//.tt/}"
    new_name="${new_name//.te/}"
    new_name="${new_name//.th/}"
    new_name="${new_name//.bo/}"
    new_name="${new_name//.ti/}"
    new_name="${new_name//.to/}"
    new_name="${new_name//.ts/}"
    new_name="${new_name//.tn/}"
    new_name="${new_name//.tum/}"
    new_name="${new_name//.tr/}"
    new_name="${new_name//.tk/}"
    new_name="${new_name//.uk/}"
    new_name="${new_name//.ur/}"
    new_name="${new_name//.ug/}"
    new_name="${new_name//.uz/}"
    new_name="${new_name//.ve/}"
    new_name="${new_name//.vi/}"
    new_name="${new_name//.war/}"
    new_name="${new_name//.cy/}"
    new_name="${new_name//.fy/}"
    new_name="${new_name//.wo/}"
    new_name="${new_name//.xh/}"
    new_name="${new_name//.yi/}"
    new_name="${new_name//.yo/}"
    new_name="${new_name//.zu/}"
    
    if [ "$file" != "$new_name" ] && [ ! -e "$new_name" ]; then
        mv -- "$file" "$new_name"
        echo "Renamed: $file -> $new_name"
    fi
done
